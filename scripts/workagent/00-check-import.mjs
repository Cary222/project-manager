/**
 * Phase 0 - Test 0: Check Import
 * 验证包能否正确导入
 */

import { createAgentSession } from "@earendil-works/pi-coding-agent";

console.log("=== Test 0: Check Import ===\n");
console.log("✅ createAgentSession imported:", typeof createAgentSession);
console.log("✅ Package loads correctly");
