# 向量搜索静默失败修复

> 适用：project-manager 仓库（Next.js + Prisma + pgvector）
> 目标：任何同事 / 未来的我拿到这份文档 + 仓库 commit 后，能**完整复现**此次 bug 排查 + 修复的端到端过程。

---

## 1. 目标 & 背景

### 1.1 旧版的问题

**现象**：部分 PKM 笔记（如 `CH585M芯片手册`）在全局向量搜索中无法被命中，但同项目其他笔记正常。

**业务影响**：用户粘贴了笔记正文片段（如 "BLE"）也无法搜到，怀疑搜索引擎坏了。

**根因链路**：

```
upsertSearchDocument()
    ↓
ensureSearchDocumentEmbedding()  ← embedding 服务调用失败（超时等）
    ↓
被 try/catch 吞掉，只打 console.error
    ↓
content 写入 DB ✅  但  embedding 列永远是 NULL
    ↓
向量搜索依赖 pgvector <-> embedding 匹配，该笔记永远在向量结果之外
    ↓
keyword 搜索能兜底搜到（如果 content 包含关键词），但向量语义搜索失效
```

### 1.2 结论

- `upsertSearchDocument` 的 embedding 写入失败**不再静默吞掉**，而是**抛出异常**
- 各 sync 函数（ticket / commit / note）各自吞掉 embedding 失败（content 已入库），并打 error log 供监控
- `backfillSearchDocuments` 改用 `Promise.allSettled`，单条失败不影响整批

---

## 2. 改动清单

| 文件 | 类型 | 作用 |
|------|------|------|
| `features/knowledge/lib/search.ts` | 修改 | 修复静默失败逻辑，4 个函数改动 |
| `lib/git-sync/scan.ts` | 修改 | 改调 `syncCommitSearchDocument`，不再直接调 `upsertSearchDocument` |

---

## 3. 核心实现

### 3.1 `upsertSearchDocument` — 抛出异常，不再静默

```471:482:features/knowledge/lib/search.ts
  try {
    const state = await getSearchDocumentEmbeddingState(document.id);
    if (state) {
      await ensureSearchDocumentEmbedding(state);
    }
  } catch (error) {
    const msg = error instanceof Error ? error.message : String(error);
    console.warn(`[search:upsert] embedding failed for ${document.id} (${document.title}): ${msg}`);
    throw error;
  }

  return document;
}
```

**为什么这样写**：embedding 服务失败（如超时 / 网络抖动）时，`upsert` 本身成功但向量写不进去。旧版把错误吞掉导致 content 和 embedding 永久不一致。新版让错误向上冒泡，由各调用方自行决定容错策略。

---

### 3.2 `syncTicketSearchDocument` / `syncCommitSearchDocument` — 吞错打 log，返回 null

```508:514:features/knowledge/lib/search.ts
  const record = buildSearchableTicketDocument(ticket);
  try {
    return await upsertSearchDocument(record);
  } catch (error) {
    console.error(`[search:syncTicket] embedding failed for ticket ${ticketId}:`, error instanceof Error ? error.message : String(error));
    return null;
  }
}
```

```541:547:features/knowledge/lib/search.ts
  const record = buildSearchableCommitDocument(commit);
  try {
    return await upsertSearchDocument(record);
  } catch (error) {
    console.error(`[search:syncCommit] embedding failed for commit ${commitId}:`, error instanceof Error ? error.message : String(error));
    return null;
  }
}
```

**为什么这样写**：ticket 和 commit 的保存不能被 embedding 故障阻断。content 已入库后降级为 keyword-only 搜索，比完全不可搜索好。

---

### 3.3 `syncPkmNoteSearchDocument` — 吞错但 fallback 返回已保存的 document

```550:577:features/knowledge/lib/search.ts
export async function syncPkmNoteSearchDocument(noteId: string) {
  const note = await prisma.pkmNote.findUnique({ ... });
  if (!note) { ... }

  const attachments = normalizePkmAttachments(note.attachments);
  const attachmentTexts = await extractAttachmentTexts(attachments);

  const record = await buildSearchablePkmNoteDocument({ ...note, attachments }, attachmentTexts);
  try {
    return await upsertSearchDocument(record);
  } catch (error) {
    const msg = error instanceof Error ? error.message : String(error);
    console.error(`[search:syncNote] embedding failed for note ${noteId} (${note.title}): ${msg}`);
    // content 已写入 DB，embedding 失败时降级：只靠 keyword 搜索，vector 部分为空
    // 重新查一次返回已保存的 document
    const saved = await prisma.searchDocument.findFirst({
      where: {
        sourceType: buildSearchDocumentSourceType(SEARCH_DOCUMENT_SOURCE_TYPES.PKM_NOTE),
        sourceId: noteId,
      },
    });
    return saved;
  }
}
```

**为什么这样写**：笔记保存 API 的调用方（如 `POST /api/pkm/notes`）依赖返回值更新前端状态，不能返回 null。fallback 查询返回已保存的 document，保证 API 不报错。

---

### 3.4 `backfillSearchDocuments` — `Promise.all` → `Promise.allSettled`

```617:637:features/knowledge/lib/search.ts
  const ticketResults = await Promise.allSettled(
    tickets.map((ticket) => upsertSearchDocument(buildSearchableTicketDocument(ticket))),
  );
  const commitResults = await Promise.allSettled(
    commits.map((commit) => upsertSearchDocument(buildSearchableCommitDocument(commit))),
  );
  const noteResults = await Promise.allSettled(
    notes.map(async (note) => {
      const attachments = normalizePkmAttachments(note.attachments);
      const attachmentTexts = await extractAttachmentTexts(attachments);
      return upsertSearchDocument(
        await buildSearchablePkmNoteDocument({ ...note, attachments }, attachmentTexts),
      );
    }),
  );

  const errors = [
    ...ticketResults.filter((r): r is PromiseRejectedResult => r.status === "rejected"),
    ...commitResults.filter((r): r is PromiseRejectedResult => r.status === "rejected"),
    ...noteResults.filter((r): r is PromiseRejectedResult => r.status === "rejected"),
  ];
  if (errors.length > 0) {
    console.warn(`[search:backfill] ${errors.length} items failed embedding, content saved without vector`);
  }

  return {
    tickets: ticketResults.length,
    commits: commitResults.length,
    notes: noteResults.length,
    errors: errors.length,
  };
}
```

**为什么这样写**：旧版 `Promise.all` 任一失败全部失败，导致 batch 重建中断。`Promise.allSettled` 让成功的继续，失败的单独收集，最后输出错误数量。

---

### 3.5 `lib/git-sync/scan.ts` — 改用 `syncCommitSearchDocument`

```8:12:lib/git-sync/scan.ts
  buildSearchableCommitDocument,
  backfillSearchDocuments,
  syncCommitSearchDocument,
} from "@/shared/lib/search";
```

```120:121:lib/git-sync/scan.ts
    if (linkedCommit) {
      await syncCommitSearchDocument(linkedCommit.id);
    }
```

**为什么这样写**：直接调 `upsertSearchDocument` 会因 embedding 失败导致 git scan 中断。改用 `syncCommitSearchDocument` 已有错误处理，保证 git sync 流程不被阻断。

---

## 4. 环境与配置

| 变量 / 依赖 | 值 | 说明 |
|------------|----|------|
| `DATABASE_URL` | `postgresql://community:community@192.168.1.14:5432/community` | 远程 DB，含 pm schema |
| `EMBEDDING_API_URL` | `http://192.168.1.14:5000` | 向量服务，1024 维 |
| pgvector 扩展 | `public.vector` | DB 侧向量类型 |

---

## 5. 启动 / 部署

无需额外部署。代码改动在 Next.js 服务中，重启服务即可生效：

```bash
# 1. 确认改动已部署到服务器
ssh hxy@192.168.1.14 "cd /home/hxy/work/personal/project-manager && git pull"

# 2. 重启 Next.js 服务（端口 3003）
ssh hxy@192.168.1.14 "cd /home/hxy/work/personal/project-manager && pm2 restart pm"
```

---

## 6. 测试 & 验证

### 6.1 诊断脚本验证（本地）

```bash
cd /Users/vastgui/Desktop/project-manager

# 跑诊断脚本（会自动重建 CH585M 的 embedding）
npx tsx scripts/diagnose-ch585m-bug.ts
```

**期望输出**：

```
=== CH585M 搜索不到 — 诊断 ===

[1] 笔记信息
    id:       cmq6ghljh0001jln98ih58my0
    title:    CH585M芯片手册
[2] SearchDocument 状态
    embedding:     有 (1024 dims)          ← 之前是 0，现在有值
[4] 搜索 "CH585M"（全局）
    ✅ [note] CH585M芯片手册              ← 能搜到
[7] 重建后搜索 "CH585M"
    ✅ CH585M 笔记在结果中！修复成功。
```

### 6.2 直接验证搜索 API

```bash
curl -s "http://localhost:3003/api/search?q=CH585M&limit=8" \
  -H "Cookie: session=<你的 session>" | jq '.results[] | select(.type=="note") | {title, score}'
```

**期望输出**：`CH585M芯片手册` 在结果中，score > 10。

### 6.3 验证 embedding 已写入 DB

```bash
# SSH 到服务器查
ssh hxy@192.168.1.14 'psql "$DATABASE_URL" -t -c "
SELECT title, (embedding IS NOT NULL) as has_emb FROM pm.\"SearchDocument\" WHERE title LIKE '\''%CH585M%'\'';
"'
```

**期望输出**：`CH585M芯片手册 | t`

---

## 7. 复现 Checklist

- [ ] 在本地拉取最新代码 `git pull`
- [ ] 确认 embedding 服务在 `http://192.168.1.14:5000` 可访问（`curl http://192.168.1.14:5000/dimension` 返回 `{"dimension":1024}`）
- [ ] 确认 `.env.local` 的 `DATABASE_URL` 和 `EMBEDDING_API_URL` 正确
- [ ] 跑诊断脚本 `npx tsx scripts/diagnose-ch585m-bug.ts`，看到 `[4] 搜索 "CH585M"` 出现 `✅ [note] CH585M芯片手册`
- [ ] 浏览器登录 `http://localhost:3003`，全局搜索 "CH585M"，确认笔记出现在结果中
- [ ] 搜索笔记正文关键词 "BLE"，确认笔记出现在结果中
- [ ] 验证 embedding 已写入 DB（`SELECT (embedding IS NOT NULL) FROM pm."SearchDocument" WHERE ...`）
- [ ] 服务器端 git pull + pm2 重启
- [ ] 再次浏览器验证搜索正常

---

## 常见坑（避雷）

1. **embedding 服务不可达时 content 仍然写入** — 这是预期降级行为，不算 bug，keyword 搜索仍可用
2. **`syncPkmNoteSearchDocument` 返回 `null` 的语义** — 旧版是"笔记不存在"，新版在 embedding 失败时也返回 null（fallback 查询），调用方需注意
3. **`refreshSearchDocumentEmbeddings` 查 `embedding IS NULL`** — Prisma schema 里 `embedding` 是 `Unsupported("public.vector")`，不能通过 Prisma ORM select，必须用 `$queryRaw`；如果 `SELECT (embedding IS NOT NULL)` 全返回 true，说明修复成功
4. **诊断脚本需要 `.env.local`** — 本地开发时确保有 `DATABASE_URL` 和 `EMBEDDING_API_URL`，否则 embedding 调用会报 `EMBEDDING_API_URL_MISSING`
