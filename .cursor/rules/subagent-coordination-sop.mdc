---
description: 子代理协作 SOP — 主代理是唯一协调者，子代理间禁止直接通信/互相监控
globs: .cursor/agents/**/*.md
alwaysApply: true
---

# 子代理协作 SOP（主代理调度模式）

## 核心理念

> **Main 拆分任务、控制边界、协调流程、做最终决策；Subagent 仅在明确的 Scope 内执行和建议，不负责调度、协调或流程控制。**

## 核心原则

> **主代理是唯一协调者。子代理间禁止直接通信。**

Cursor 的 `Task` 子代理机制特点：

- 每个子代理有**独立的 chat context**，无法看到其他子代理的状态
- 子代理的 prompt 只能来自**主代理**或被另一子代理**显式同步调用**
- 子代理**没有自动触发 / 跨代理事件订阅 / 跨代理状态查询**能力
- 子代理 prompt 中的"等 X 完成后再做 Y"等时序语言**无法生效**

---

## Mode C — 小任务默认路径（降级）

**触发条件**：任务不可拆分 或 预计修改不超过 3 个文件（修 Bug / 改按钮 / 改 Prisma 字段 / 改 README / 改注释）

> 这是 Cursor 最稳定、Token 最少的模式。避免所有事情都走 6 阶段。

```text
Main 收到需求
  ↓
判断：≤3 文件 且 无跨 feature 依赖？
  ↓ Yes → Mode C
  ↓ No  → 进入主流程（Stage0）
```

Mode C 流程：

```text
Main
  ↓
fullstack-developer（Foreground）
  ↓
实现 → 自测 → npx tsc --noEmit
  ↓
Final Response
  ↓
Main 验收 → User Decide Commit
```

---

## 主流程 — 6 阶段（仅在 Mode C 不适用时启用）

```text
Stage0  Task Analysis（Main 自己，文件边界互斥）
Stage1  Advisor Review（ai-learning-mentor，输出 APPROVED/CHANGES_REQUIRED/BLOCKED）
Stage2  Feature Fan-out（每个 feature：Dev → Main 判断 → Review?）
Stage3  Merge + Smoke Review（Main 决策，fullstack-developer 执行 Git）
Stage4  Document（dev-to-doc-recap skill）
Stage5  User Decide → Commit（Agent 执行 Git）
```

### Stage0 — Task Analysis（Main 自己）

主代理收到需求后，**自己**完成：

1. 读 `pm-dev/PROJECT-HUB.md` / `feature-first` skill 摸项目现状
2. **判断是否走 Mode C**：≤3 文件 且 无跨 feature 依赖 → Mode C，退出主流程
3. 识别独立 feature 边界（哪些可并行 / 哪些有强依赖必须串行）
4. 写出 feature 列表 + 每个 feature 的输入输出 + **依赖图**
5. **列出共享文件**（共用类型 / 共用 Prisma schema / 共用 lib）
6. **文件边界互斥**：每个文件同时只属于一个 Feature

**关键约束：**
- 一个文件同时只属于一个 Feature
- 共享文件由 Main 在 Stage3 合并，不派给子代理

输出 `## 任务拆分摘要`，作为 Stage1 的输入。

### Stage1 — Advisor Review（ai-learning-mentor，Foreground）

- `run_in_background: false`
- prompt 必含：
  - `## 任务背景`：Stage0 的 `## 任务拆分摘要`
  - `## 必读产物路径`：pm-dev/PROJECT-HUB.md、待改文件
  - `## 方案请求`：review feature 拆分是否合理 / 跨 feature 边界是否漏 / 风险点

**Advisor 输出固定三档（Main 直接 if 判断）：**
- `APPROVED`：直接进入 Stage2
- `CHANGES_REQUIRED`：补 X 后再执行（Main 调整方案，最多 2 次）
- `BLOCKED`：方案有根本问题，重新设计

### Stage2 — Feature Fan-out

每个 feature 独立走：

```text
Feature1: fullstack-developer → Main 判断 → （需要 Review?）→ code-reviewer + ai-learning-mentor
Feature2: fullstack-developer → Main 判断 → （需要 Review?）→ code-reviewer + ai-learning-mentor
Feature3: fullstack-developer → Main 判断 → （需要 Review?）→ code-reviewer + ai-learning-mentor
```

**Review 触发条件（Main 判断，保守原则）：**
- ✅ 涉及业务逻辑 / 数据库 / 架构 / 权限 / API → **必须 Review**
- ❌ 改 README / 改注释 / 改样式细节 / 纯文案修改 → **跳过 Review**
- 不确定 → 走 Review

每个 fullstack-developer prompt 必含：
- `## 任务背景`：此 feature 的具体范围 + Stage0 摘要
- `## 必读产物路径`：此 feature 涉及的所有文件
- `## 范围限定`：明确"只动这个 feature，不要越界改其它 feature"
- `## 文件边界互斥`：此 feature 修改的文件列表（Stage0 已保证无重叠）
- `## 必跑命令`：`npx tsc --noEmit` 必须绿
- `## 汇报格式`：文件清单 + tsc 结果 + 踩坑记录

每个审查 prompt 必含：
- `## 必读产物路径`：对应 feature 的产物文件
- `## 范围限定`：硬层 vs 软层（code-reviewer / ai-learning-mentor）
- `## 报告输出文件`

主代理在每个 feature 收到：
- Dev Final Response
- 审查报告（如需 Review）
- Critical (Must Fix) → 派 fullstack-developer 修复（Foreground，单 agent）
- 修复完重跑 `npx tsc --noEmit` 必须绿

### Stage3 — Merge + Smoke Review

**Main 决策合并策略 → 派 fullstack-developer 执行 Git 操作**：

> ⚠️ **Git 操作必须遵循 `git-commit-required.mdc` 纪律**，包括：问单号 / 加 Co-authored-by / 默认推 origin。详见该规则文件。

```text
Main
  ↓
决策合并策略（rebase / merge / squash）
  ↓
派 fullstack-developer 执行：
  - 按 git-commit-required.mdc Step 7 精确 add feature 产物
  - git diff --check（检查冲突）
  - git merge / rebase（解决冲突）
  ↓
Smoke Review（code-reviewer）：
  - 只检查整体一致性
  - 检查 Stage2 是否漏 Review 关键改动
  - 不重做完整 review（Stage2 已覆盖）
  ↓
如有问题 → 回 Stage2 对应 feature 修复
```

**关键约束**：
- Main 不直接操作 Git，**所有 Git 命令由 fullstack-developer 执行**，且必须遵循 `git-commit-required.mdc`
- Main 只做**决策**（用什么策略合并），Agent 执行

### Stage4 — Document

- 主代理用 `dev-to-doc-recap` skill 生成 8 段式复现文档
- 输出：`docs/reports/PR<N>-<name>.md`
- 必含 8 段：背景 / 目标 / 设计 / 实现 / 测试 / 上线 / 踩坑 / 后续

### Stage5 — User Decide → Commit

- 主代理把 PR 摘要 + 审查报告 + 复现文档路径给用户
- **Subagent 不建议跳过此步骤**（Authority Boundary）
- 用户决定后：
  - commit → 派 fullstack-developer 执行 git commit（**按 `git-commit-required.mdc` 纪律**：问单号 / 加 Co-authored-by / 默认推 origin）
  - 不 commit → 保留工作树

---

## Rules

### Rule 7 — Task 调用策略

> **默认 Foreground，无依赖任务才用 Background。**
>
> - 所有子代理调用默认 `run_in_background: false`
> - 多 agent 在同轮调用（同一 message 内多次 Task 调用），Cursor Runtime 自行决定执行策略（串行或并发），SOP **不依赖** 具体调度方式
> - 仅当任务结果不是后续步骤前置条件时，才使用 Background
> - 理由：Cursor 不保证 Background Final Response 回流（已踩坑）

### Rule 8 — 子代理汇报路径与时序约束

> Subagent Final Response **只发给主代理**，绝不写"等 X 子代理完成后做 Y"。
>
> 跨子代理时序由主代理在 prompt 里写明产物路径，不让子代理自己协调。

### Rule 9 — 异常恢复（仅作异常手段）

> Foreground 调用正常流程一定会拿到 Final Response。仅当 Task 返回内容异常 / 不完整时：
>
> 1. **Read transcript 一次**：`/Users/vastgui/.cursor/projects/<workspace>/agent-transcripts/<shell_id>.jsonl` 最后一条 assistant 消息
> 2. **如果仍异常**：重新派发（仍然 Foreground），prompt 加"上轮产物路径 + 已知问题"
> 3. **禁止**：多次 Read（轮询）/ Resume 已完工 Task / 按文件大小或关键字判断完工

### Rule X — Authority Boundary

> **Main 决策，Subagent 建议。**
>
> Subagent 只能**建议**，不能**执行流程**：
>
> - 禁止跳过任何 Stage（如"建议跳过 Review 直接 Commit"）
> - 禁止替 Main 决定是否修复 / 是否继续 / 是否合并
> - 禁止操作 Git（`git add/commit/merge` 由 Main 决策 + fullstack-developer 执行，**必须遵循 `git-commit-required.mdc`**）
>
> Subagent 的输出是**建议**，最终决策权归主代理。

### Rule Y — Immutable Input

> **派发后的 Feature Scope 不可修改。**
>
> 派发 Feature 后，Feature 的 Scope（涉及文件 / 任务范围 / 验收标准）冻结：
>
> - 新增需求必须新建 Feature，由 Main 重新派发
> - 修改需求必须新建 Feature，不允许改原 Feature 的 Prompt
> - 不同 Agent 拿到不同 Prompt 是混乱的根源

---

## 错误模式（禁止）

❌ **跨子代理时序**

```text
// ❌ 错：子代理 B 指望"PR2 完成后做审计"
Task(fullstack-developer) — 实施 PR2
Task(ai-learning-mentor) — 等 PR2 完成后做审计
// ↑ 子代理 B 拿到 prompt 时只能 idle 等指令
```

❌ **子代理里写"监控其他子代理"**

```text
"PR2 子代理完成后 10 分钟内开始审计"
"轮询 transcript 大小，等待完成信号"
```

❌ **Subagent 越权**

```text
"我觉得不用 Review"
"我建议直接 Commit"
"跳过 Stage3，直接合并"
// → Subagent 只能建议，Main 决策
```

❌ **派发后修改 Scope**

```text
// ❌ Feature A 做到一半，再加一个 XX 功能
// → 新需求必须新建 Feature
```

---

## 主代理编排 checklist

- [ ] 任务走 Mode C（≤3 文件）还是主流程（>3 文件 / 多 feature）
- [ ] 每个 feature 的文件边界互斥（Stage0 已确认）
- [ ] 每个子代理 prompt 都包含 `## 必读产物路径` 和 `## 范围限定`
- [ ] Stage2 Review 触发条件已判断（业务/DB/架构/权限/API 才 Review）
- [ ] Stage3 Merge 策略已定（Main 决策），Git 命令派给 fullstack-developer（**遵循 `git-commit-required.mdc`**）
- [ ] Main 负责决策，不越权执行 Git / 不跳过 Stage / 不改派发后的 Scope

---

## 子代理 prompt 模板

```text
你是 <role> 子代理。
[角色身份 + 必读规则]

## 任务背景
[主代理传入的上下文：需求、已拍板的决策、产物路径]

## 你的工作
[具体步骤 1、2、3]
[每一步要 Read / Edit / Bash 的具体文件]

## 不要做的事
[明确禁止]

## 汇报格式
[主代理需要什么形式的最终响应]

## ⚠️ 重要约束
- 你的最终响应只发给主代理
- 不要写"等 X 子代理完成后做 Y"——你不知道 X 的状态
- 如果收到 prompt 时需要等前置产出，请向主代理汇报"缺少前置产物"而不是 idle 等
- 你只能建议，不能执行流程（Authority Boundary）
```

---

## 一句话总结

> **Main 拆分任务、控制边界、协调流程、做最终决策； Subagent 仅在明确的 Scope 内执行和建议，不负责调度、协调或流程控制。**
