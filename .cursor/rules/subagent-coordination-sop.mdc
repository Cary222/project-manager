---
description: 子代理协作 SOP — Main 是协调者也是执行者，子代理 Skill 是专业提示词合集
globs: .cursor/agents/**/*.md
alwaysApply: true
---

# 子代理协作 SOP

## 一句话原则

> Main 拆任务、定边界、做决策，也是执行者；Subagent 只在需要隔离上下文或复杂任务时才派。
> 产物落盘是可靠完工信号；transcript 是辅助参考。

---

## Main 执行模式

Main 不只是协调者，也是万能执行者。两种执行方式：

| 方式 | 何时用 |
|---|---|
| **直接执行** | 小任务（≤3 文件）、review 报告、Stage0 分析、文档整理 |
| **派 Task** | 复杂 feature 实施、需要隔离上下文、多 feature 并行 |

**直接执行流程**：读对应专精的子代理 skill → Main 作为该身份执行 → 产物落工作区。

**Main 主动干活原则**：等子代理期间，Main 直接执行不依赖该子代理产物的任务，不 idle。

### 并行派发汇合策略

并行派发多个子代理时：

1. **派发后立即继续** — Main 不等子代理回报，继续执行其他不依赖这些子代理的任务
2. **定期检查工作区** — 按完工信号三结合（产物文件 > git status > transcript）判断每个子代理是否完成
3. **全部齐全后合并** — 所有产物齐全才执行合并，不依赖子代理主动回报
4. **禁止**：在并行派发后"等待所有子代理"然后什么都不做

---

## 模式判断

| 模式 | 条件 | 流程 |
|---|---|---|
| **Mode C** | ≤3 文件、无跨 feature 依赖 | 直接执行 or 派 fullstack-developer → git diff 验收 → User Decide |
| **Mode M** | 单 feature、业务逻辑 | 派 fullstack-developer → 双审查 → Main 合并 → User Decide |
| **Mode L** | 多 feature | Stage0 拆分 → 每个走 Mode M → Stage3 合并 |

---

## Stage0 — Task Analysis（Main 直接执行）

1. 读 `pm-dev/PROJECT-HUB.md` / `feature-first` skill 摸项目现状
2. 判断走哪个 Mode
3. 识别 feature 边界：可并行 vs 必须串行
4. 列出共享文件
5. **文件边界互斥**：每个文件同时只属于一个 Feature

输出 `## 任务拆分摘要`。

---

## Stage1 — Advisor Review

**方式**：直接执行（读 ai-learning-mentor skill）或派 Task。

**固定三档输出**：
- `APPROVED` → 进入 Stage2
- `CHANGES_REQUIRED` → Main 调整方案，最多 2 次
- `BLOCKED` → 重新设计

---

## Stage2 — Feature Fan-out

每个 feature 走：

```
派 fullstack-developer（Foreground）
  ↓
双审查（code-reviewer + ai-learning-mentor，Foreground，各写不同文件）
  ↓
Main 合并两份审查报告
  ↓
Critical 修复 → 直接执行或派 fullstack-developer → tsc 必须绿
```

**审查产物文件互斥（强制）**：

| Agent | 产物文件 | 文件头 |
|---|---|---|
| `code-reviewer` | `docs/reviews/PR<N>-<feature>-code-reviewer.md` | `<!-- reviewer: code-reviewer (硬层) -->` |
| `ai-learning-mentor` | `docs/reviews/PR<N>-<feature>-ai-mentor.md` | `<!-- reviewer: ai-learning-mentor (软层) -->` |

Main 在 Stage2 末尾合并为 `docs/reports/PR<N>-<feature>-review.md`，标注 merged by Main。

**Review 触发条件**：业务逻辑 / 数据库 / 架构 / 权限 / API → 必须；README / 注释 / 样式 / 纯文案 → 跳过。

---

## Stage3 — Merge + Smoke Review

- Main 决策合并策略（rebase / merge / squash）
- **Git 操作只由 fullstack-developer 执行**，遵循 `git-commit-required.mdc`
- Smoke Review（直接执行 code-reviewer）：只检查整体一致性

---

## Stage4 — Document（按需）

- 用 `dev-to-doc-recap` skill 生成复现文档

---

## Stage5 — User Decide → Commit

- Main 把 PR 摘要 + 审查报告给用户，**不可跳过**
- 用户决定后派 fullstack-developer 执行 git commit（按 `git-commit-required.mdc`）

---

## 完工信号（三结合判断）

判断子代理 / stage 是否完工：

1. **产物文件**（首选）：`git status` / `git diff` / `Glob docs/**/*.md`
2. **git status**（次选）：子代理修改的文件是否出现
3. **transcript**（辅助，仅用于异常诊断）：读 `agent-transcripts/<id>.jsonl` 最后一条 assistant 消息

三结合都查不到产物 → 子代理可能卡住，按 Rule 3 异常恢复处理。

---

## 核心 Rules

**Rule 1 — 默认 Foreground**
- 所有派 Task 默认 `run_in_background: false`
- 仅当任务结果不是后续前置条件时，才用 Background

**Rule 2 — 子代理只发 Main**
- Subagent Final Response 只发给主代理
- 跨子代理时序由 Main 在 prompt 里写明产物路径

**Rule 3 — 异常恢复**
- Foreground 正常流程会拿到 Final Response
- 仅在产物文件 / git status 无法确认时，读 transcript 一次诊断
- 仍异常 → 重新派发（Foreground），prompt 加"上轮产物路径 + 已知问题"
- **禁止**：多次轮询 transcript / Resume 已完工 Task / 按文件大小判断完工

**Rule 4 — Authority Boundary**
- Subagent 只能建议，不能执行流程
- 禁止跳过 Stage / 替 Main 决定 / 操作 Git

**Rule 5 — Immutable Input**
- 派发后的 Feature Scope 不可修改
- 新增 / 修改需求必须新建 Feature

**Rule 6 — 审查产物互斥**
- 同 feature 多审查 agent 写入不同文件，Main 合并

---

## 禁止模式

- ❌ 子代理写"等 X 完成后做 Y"
- ❌ Main idle 等子代理，不主动干活
- ❌ 仅用 transcript 大小轮询等待（辅助诊断可以，轮询等待不行）
- ❌ Subagent 替 Main 决定流程
- ❌ 派发后改 Feature Scope

---

## Main 自检 checklist

- [ ] 走哪个 Mode（Mode C / M / L）
- [ ] 每个 feature 文件边界互斥
- [ ] 子代理 prompt 含：任务范围 + 必读路径 + 必跑命令 + 禁止项 + 汇报格式
- [ ] 双审查产物路径不同（Rule 6）
- [ ] Git 操作派 fullstack-developer（遵循 git-commit-required.mdc）
- [ ] Main 在等待期间直接执行其他任务，不 idle
- [ ] 并行派发后有汇合策略（Main 定期检查，不等子代理回报）
