---
description: Cursor 3.11+ 子代理协作 SOP；Main 负责策略、边界、验收与决策
alwaysApply: true
---

# 子代理协作 SOP

## 核心原则

> Main 选择执行策略、确认边界并验收结果，也是执行者。Cursor 可自动拆分任务，但不能替 Main 决定依赖、范围和质量门。
> 子代理完成通知触发汇合；`git diff`、产物文件和质量命令决定是否验收通过。

## 执行模式

| 方式 | 适用条件 |
|---|---|
| **Main 直接执行** | 小任务（≤3 文件）、分析、报告、文档整理 |
| **派 Task** | 复杂实施、需要专门能力或上下文隔离 |
| **`/multitask`** | 两个以上无前置依赖、文件边界互斥的子任务 |

- `/multitask` 不限单 feature 或多 feature；API、UI、schema 等独立子任务也可并行。
- Cursor 可以提出拆分，Main 必须确认依赖、负责文件和共享文件；不满足并行条件就顺序执行。
- 纯研究或方案讨论可建议用户使用 Side Chat，结论明确后再回主线执行。

## 模式判断

| 模式 | 条件 | 默认流程 |
|---|---|---|
| **Mode C** | ≤3 文件、低风险、无跨模块依赖 | Main 直接执行 → 验收 |
| **Mode M** | 单 feature、含业务逻辑 | 顺序或 `/multitask` → Review → 修复 → 验收 |
| **Mode L** | 多 feature 或跨模块改动 | Main 确认拆分 → 顺序或 `/multitask` → 集成 Review → 验收 |

## Stage 0 — Analysis

1. 读取项目相关 skill 和现有实现，判断 Mode、风险与质量门。
2. 标明任务依赖、负责文件和共享文件；同一文件同时只归一个执行者。
3. UI 需求在此识别类型并指定项目 UI skill；本项目使用 `pretty-ui`。
4. 仅在存在架构取舍、跨模块契约或高风险方案时做 Advisor Review；输出 `APPROVED`、`CHANGES_REQUIRED` 或 `BLOCKED`。

输出简短的任务拆分与执行策略。

## Stage 1 — Execution

1. Main 选择顺序执行、单个 Task 或 `/multitask`。
2. UI 实施前读取项目 UI skill；需要精确页面上下文时，建议用户进入 Design Mode 圈选元素后再开发。
3. 并行期间，Main 只做不依赖子代理、且不修改其负责文件的工作；优先准备 Review、文档、分析或下一阶段 Planning。
4. 以完成通知触发汇合，以 `git diff`、约定产物和质量命令验收。transcript 仅用于异常诊断，不轮询等待。

## Stage 2 — Review And Fix

1. 所有实现完成并验收后才能启动 Review；禁止 reviewer 与被审查的实现同时运行。
2. 业务逻辑、数据库、架构、权限或 API 改动必须审查；纯文案、注释或样式可跳过。
3. 硬层与软层双审查确有需要时可并行，但分别写入：
   - `docs/reviews/PR<N>-<feature>-code-reviewer.md`
   - `docs/reviews/PR<N>-<feature>-ai-mentor.md`
4. Main 汇总结论；Critical 必须修复并重新运行质量门。
5. Mode L 完成后做一次集成 Smoke Review，只检查跨模块一致性。

## Stage 3 — User Decide

1. Main 向用户提供改动摘要、验证结果、审查结论和剩余风险；不可跳过。
2. 需要复现文档时使用 `dev-to-doc-recap` skill。
3. 用户明确要求提交后，按 `git-commit-required.mdc` 和 `git-commit-assistant` skill 执行；不得擅自 commit 或 push。

## 核心约束

- **依赖决定前后台**：后续步骤依赖结果时前台执行；互相独立且 Main 有其他工作时才异步并行。
- **Authority Boundary**：子代理只执行或建议，不替 Main 跳过阶段、扩大范围或决定 Git 操作。
- **Immutable Scope**：派发后不得修改任务范围；新增需求另建任务。
- **文件互斥**：并行执行者不得修改同一文件；双审查产物路径必须不同。
- **异常恢复**：完成通知与工作区不一致时读一次 transcript；仍异常才重新派发，并附上已知问题和预期产物。

## Main 自检

- [ ] Mode、风险和质量门已确定
- [ ] 顺序或 `/multitask` 的选择有明确依据
- [ ] 并行任务无前置依赖，负责文件互斥
- [ ] UI 实施已加载对应项目 UI skill
- [ ] 子代理 prompt 包含范围、必读路径、必跑命令、禁止项和汇报格式
- [ ] 实现已汇合并验收后才启动 Review
- [ ] Main 等待期间未修改子代理负责文件
- [ ] 用户已看到摘要与审查结论，再决定是否提交
