---
name: AI 模型配置层完整实施方案（合并版）
overview: 合并两个方案：用户倒入自定义模型 + API Key，用户可选手动切换模型 / 配置默认路由规则，系统按任务类型智能路由 + 用户可覆盖。对接中转平台 long-term plan，在 LangGraph 层加 model-select 节点，State 存 modelContext 而非硬编码模型名。参考 byok-custom-model / shadcn-aisdk-model-select / settings-api-keys / @nocoo/next-ai / 中转平台 Stage 6。
todos:
  - id: s1
    content: "Stage 1: Model Registry 服务端（providers/types + registry + Agnes/DeepSeek Provider）"
    status: pending
  - id: s2
    content: "Stage 2: SettingsCenter AI 模型配置区块（ROOT 配置默认模型，用户倒入自定义模型）"
    status: pending
  - id: s3
    content: "Stage 3: Chat 界面手动模型切换 + localStorage 记忆偏好"
    status: pending
  - id: s4
    content: "Stage 4: LangGraph graph 加 model-select 节点（按任务类型路由）"
    status: pending
  - id: s5
    content: "Stage 5: API 支持 modelName 参数 + generate-response 动态模型"
    status: pending
  - id: s6
    content: TypeScript 编译 + smoke test
    status: pending
isProject: false
---

# Plan: AI 模型配置层完整实施方案（合并版）

> 本方案合并自：
> - `ai_模型切换_+_配置层（对接中转平台长期规划）_71991c1e.plan.md`
> - `ai_模型切换_+_用户自配置功能实施方案_cfcbd728.plan.md`
> - 用户反馈：Model Registry + User Routing Config + LangGraph model-select 节点

---

## 需求定位

这不是一个独立功能，而是**中转平台 Stage 6（内置 AI 灰度切换）的最小前端实现**，同时满足用户自定义模型导入需求。

```
中转平台 Stage 6 文件规划（ai中转平台长期建设_a376dc39.plan.md）：
features/ai/lib/
├── agnes-provider.ts          # 保留回滚 ✅ 已存在
├── model-provider.ts          # 新增：按配置创建 Provider ← 即本需求
├── model-catalog.ts           # 新增：稳定模型别名
└── model-runtime-config.ts     # 新增：灰度与环境开关

Stage 6 目标：让内置 AI Graph 从 Agnes 灰度切换到 Gateway
本需求目标：用户倒入模型 + 智能路由 + 手动覆盖
```

---

## 需求澄清（用户确认版）

```
用户倒入自己的模型 + API Key（Settings 页）
    ↓
用户可选：手动切换模型 / 配置默认路由规则
    ↓
系统按任务类型智能路由（用户可覆盖）
```

**三层架构**：
1. **Model Registry**：管理员 + 用户可添加自定义模型
2. **User Routing Config**：用户配置默认路由规则
3. **Manual Override**：Chat 界面手动切换（覆盖路由）

**不做**：
- ❌ 自动模型评估 / AB Test / 动态成本调度
- ❌ State 里存 `model: "xxx"`，改存 `modelContext: { taskType, capability }`
- ❌ 每个节点配不同模型（复杂度太高）

---

## 架构分层（与中转平台 + LangGraph 对齐）

```
┌──────────────────────────────────────────────────────────────┐
│                        前端层                                 │
│  app/ai/page.tsx       → ModelSelector 下拉（手动覆盖）     │
│  settings/page.tsx      → AI 模型配置区块（导入 + 路由规则） │
│  localStorage           → 用户模型偏好 + 路由规则            │
└──────────────────────────────┬───────────────────────────────┘
                               │ POST { message, modelName, manualOverride }
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                     服务端层（新增）                           │
│  app/api/ai/models/route.ts       → GET 可用模型列表        │
│  features/ai/llm/model-routing.ts  → 按 taskType 路由决策     │
│  features/ai/llm/model-provider.ts  → 按配置创建模型        │
│  features/ai/llm/model-catalog.ts  → 稳定模型别名            │
│  features/ai/llm/model-runtime-config.ts → 灰度开关          │
└──────────────────────────────┬───────────────────────────────┘
                               │ createModel(providerId, modelName)
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                     Provider 层（新增）                       │
│  Agnes  → env.AGNES_API_URL / env.OPENAI_API_KEY           │
│  DeepSeek → env.DEEPSEEK_API_KEY（用户提供，先放 .env.local）│
│  ... → 后续接入中转平台 Gateway Port                         │
└──────────────────────────────────────────────────────────────┘
```

---

## LangGraph 集成架构

```
START
  ↓
detectIntent（检测意图 + 判断 taskType）
  ↓
modelSelect（新增：根据 taskType + 用户偏好路由到合适模型）
  ↓
  ├─ searchKnowledge → searchStructured
  ├─ webSearch
  └─ generateResponse（用 modelSelect 选定的模型）
  ↓
END
```

**关键点**：`modelSelect` 节点读取 `taskType` + `userRoutingConfig`，决定用哪个模型，注入到 State context。State 存 `modelContext` 而非硬编码 `model`。

---

## 参考方案对比

| 方案 | 来源 | 借鉴点 | 与本项目契合度 |
|------|------|--------|---------------|
| `byok-custom-model` skill | starchild-ai-agent (2.5K 安装) | 自定义 Provider 注册 + API Key 管理模式 | 高：Registry 设计可复用 |
| `shadcn-aisdk-model-select` | simonorzel26 | React Context + useModelSelection Hook | 高：前端状态管理参考 |
| `shadcn/settings-api-keys block` | shadcn.io | Key 掩码显示 + 验证反馈 | 高：Settings UI 参考 |
| `@nocoo/next-ai` | halfhacked/next-ai | ProviderRegistry 工厂 + Storage Adapter | 高：工厂模式直接迁移 |
| `Vercel AI SDK Providers` | ai-sdk.guide | createDeepSeek / createOpenAI 统一接口 | 高：Provider 创建模式 |
| 中转平台 Stage 6 | `ai中转平台长期建设_a376dc39.plan.md` | model-provider.ts + Gateway Port | **最高：直接复用文件路径** |

---

## 目录结构

```
features/ai/
├── llm/
│   ├── providers/
│   │   ├── types.ts              # LLMProvider / ModelCatalogEntry / RoutingConfig
│   │   ├── registry.ts           # Provider 注册表 + 工厂函数
│   │   ├── system-providers.ts   # 内置：Agnes（从现有 agnes-provider.ts 拆分）
│   │   └── user-providers.ts     # 用户自定义模型（存 .env.local）
│   ├── model-catalog.ts          # 稳定模型别名（Stage 6 对齐）
│   ├── model-runtime-config.ts   # 灰度开关（Stage 6 对齐）
│   ├── model-routing.ts          # 路由决策：根据 taskType + 规则选模型
│   └── model-selector.tsx        # 前端手动切换下拉
│
├── graph/
│   ├── state.ts                  # 改动：加 modelContext 字段
│   ├── nodes/
│   │   ├── model-select.ts       # 新增：根据 taskType 路由模型
│   │   └── generate-response.ts  # 改动：支持动态模型
│   └── edges/
│       └── routing.ts             # 改动：modelSelect → 各工具节点
│
app/
├── api/ai/
│   └── models/route.ts          # GET 可用模型列表
│
features/settings/
└── SettingsCenter.tsx           # 改动：加 AI 模型配置区块
```

---

## 实施阶段

### Stage 1：Model Registry 服务端（与中转平台 Stage 6 完全对齐）

#### 1. `features/ai/llm/providers/types.ts`

```typescript
/** 任务类型：用于模型路由决策 */
export type TaskType = "chat" | "search" | "rag" | "complex" | "quick";

/** 模型能力标签 */
export type ModelCapability = "fast" | "standard" | "strong" | "vision" | "reasoning";

export interface LLMProvider {
  id: string;
  name: string;
  provider: "openai" | "anthropic" | "deepseek" | "custom";
  baseURL?: string;
  apiKeyEnv?: string;   // 从 env 读 Key 的变量名
  models: ModelCatalogEntry[];
}

export interface ModelCatalogEntry {
  id: string;
  modelName: string;      // 实际调用名
  displayName: string;   // 用户看到的名称
  capabilities: ModelCapability[];
  maxTokens?: number;
  enabled: boolean;
}

export interface UserRoutingConfig {
  userId?: string;       // 空 = 全局默认
  defaults: Record<TaskType, string>;  // taskType → "providerId:modelName"
  manualOverride?: string; // 用户手动选的模型（优先于 defaults）
}
```

#### 2. `features/ai/llm/providers/system-providers.ts`（从 agnes-provider.ts 拆分）

```typescript
import { createOpenAI } from "@ai-sdk/openai";
import { buildProxyAwareFetch, AGNES_API_BASE_URL } from "./proxy";

// 内置 Provider（从 env 读配置）
export const SYSTEM_PROVIDERS: LLMProvider[] = [
  {
    id: "agnes",
    name: "Agnes",
    provider: "openai",
    baseURL: process.env.AGNES_API_URL ?? "https://apihub.agnes-ai.com/v1",
    apiKeyEnv: "OPENAI_API_KEY",
    models: [
      { id: "agnes-2.5-flash", modelName: "agnes-2.5-flash", displayName: "Agnes 2.5 Flash", capabilities: ["fast"], enabled: true },
      { id: "agnes-2.0-flash", modelName: "agnes-2.0-flash", displayName: "Agnes 2.0 Flash", capabilities: ["fast"], enabled: true },
    ],
  },
];

export function create AgnesModel(modelName: string) {
  return createOpenAI({
    baseURL: AGNES_API_BASE_URL,
    apiKey: process.env.OPENAI_API_KEY ?? "",
    fetch: buildProxyAwareFetch(),
  })(modelName);
}
```

#### 3. `features/ai/llm/providers/user-providers.ts`

用户自定义模型（第一版：存 `.env.local`）：

```typescript
// 用户在 Settings 页指引配置 .env.local 的 DEEPSEEK_API_KEY
export const USER_PROVIDERS: LLMProvider[] = [
  {
    id: "deepseek",
    name: "DeepSeek",
    provider: "deepseek",
    baseURL: "https://api.deepseek.com/v1",
    apiKeyEnv: "DEEPSEEK_API_KEY",
    models: [
      { id: "deepseek-chat", modelName: "deepseek-chat", displayName: "DeepSeek Chat", capabilities: ["standard"], enabled: Boolean(process.env.DEEPSEEK_API_KEY) },
      { id: "deepseek-coder", modelName: "deepseek-coder", displayName: "DeepSeek Coder", capabilities: ["standard"], enabled: Boolean(process.env.DEEPSEEK_API_KEY) },
    ],
  },
];
```

后续接 DB 或中转平台 Gateway（Stage 2 `AiAccessKey` 表）。

#### 4. `features/ai/llm/providers/registry.ts`

```typescript
import { createOpenAI } from "@ai-sdk/openai";
import { createDeepSeek } from "@ai-sdk/deepseek";
import { SYSTEM_PROVIDERS, create AgnesModel } from "./system-providers";
import { USER_PROVIDERS } from "./user-providers";
import type { LLMProvider, ModelCatalogEntry } from "./types";

export const ALL_PROVIDERS = [...SYSTEM_PROVIDERS, ...USER_PROVIDERS];

export function getProvider(id: string): LLMProvider | undefined {
  return ALL_PROVIDERS.find(p => p.id === id);
}

export function getEnabledModels(): ModelCatalogEntry[] {
  return ALL_PROVIDERS.flatMap(p => p.models.filter(m => m.enabled));
}

export function createModel(providerId: string, modelName: string) {
  const provider = getProvider(providerId);
  if (!provider) throw new Error(`Unknown provider: ${providerId}`);

  const apiKey = provider.apiKeyEnv
    ? process.env[provider.apiKeyEnv] ?? ""
    : "";

  const baseURL = provider.baseURL ?? "https://api.openai.com/v1";

  switch (provider.provider) {
    case "deepseek":
      return createDeepSeek({ apiKey, baseURL })(modelName);
    case "openai":
    default:
      return createOpenAI({ apiKey, baseURL })(modelName);
  }
}
```

#### 5. `features/ai/llm/model-runtime-config.ts`（Stage 6 对齐）

```typescript
// 与中转平台 Stage 6 的 model-runtime-config.ts 完全对齐
export const AI_CONFIG = {
  defaultProvider: process.env.AI_DEFAULT_PROVIDER ?? "agnes",
  defaultModel: process.env.AI_DEFAULT_MODEL ?? "agnes-2.5-flash",
  rolloutPercent: parseInt(process.env.AI_GATEWAY_ROLLOUT_PERCENT ?? "0"),
  gatewayBaseUrl: process.env.AI_GATEWAY_BASE_URL,
} as const;
```

---

### Stage 2：API 层 + Settings 配置区块

#### 6. `app/api/ai/models/route.ts`

```typescript
// GET /api/ai/models → 返回可用模型列表（不含 API Key）
// 供前端 ModelSelector 下拉使用
export async function GET() {
  const models = getEnabledModels();
  return Response.json({ data: models });
}
```

#### 7. `features/settings/SettingsCenter.tsx`（改动）

参考 shadcn/settings-api-keys block 设计，在 ROOT 可见区块新增 AI 配置：

```
┌─ AI 模型配置 ───────────────────────────────────────────────┐
│                                                              │
│  默认路由策略（管理员设置）                                  │
│  ├─ 快速对话（chat/quick）→ Agnes 2.5 Flash            │
│  ├─ 知识检索（search/rag）→ Agnes 2.5 Flash            │
│  └─ 复杂分析（complex）→ DeepSeek Chat                  │
│                                                              │
│  用户自定义模型                                            │
│  ├─ Agnes    ✅ 已配置  [查看端点]                       │
│  └─ DeepSeek ⏳ 未配置  [配置 API Key]                  │
│       点击配置 → 显示说明：需在服务器 .env.local 配置      │
│       DEEPSEEK_API_KEY=sk-xxxx 后重启生效                 │
│                                                              │
│  + 添加自定义模型                                          │
│    Provider: [OpenAI ▼]                                    │
│    模型名称: [deepseek-chat]                               │
│    API Key:  [用户输入 → 存入 .env.local]                │
│    Base URL: [可选]                                       │
└──────────────────────────────────────────────────────────────┘
```

**DeepSeek 配置方式（第一版）**：用户把 Key 填入服务器的 `.env.local`，Settings 页显示是否已配置（检测 `process.env.DEEPSEEK_API_KEY` 是否存在）。后续接中转平台 Gateway（Stage 6+）。

---

### Stage 3：前端手动切换下拉

#### 8. `features/ai/llm/model-selector.tsx`

参考 `shadcn-aisdk-model-select`，使用 React Context + useModelSelection Hook：

```tsx
"use client";
import { useState, useEffect } from "react";
import type { ModelCatalogEntry } from "./types";

interface ModelSelectorProps {
  value: string;
  onChange: (model: string) => void;
}

export function ModelSelector({ value, onChange }: ModelSelectorProps) {
  const [models, setModels] = useState<ModelCatalogEntry[]>([]);
  const [open, setOpen] = useState(false);

  useEffect(() => {
    fetch("/api/ai/models")
      .then(r => r.json())
      .then(d => setModels(d.data ?? []));
  }, []);

  const current = models.find(m => m.modelName === value);

  return (
    <div className="relative">
      <button
        onClick={() => setOpen(!open)}
        className="flex items-center gap-1.5 rounded-lg border border-ink-200 bg-white px-2 py-1 text-xs font-medium text-ink-700 hover:bg-ink-50"
      >
        <span>{current?.displayName ?? "选择模型"}</span>
        <ChevronDown className="h-3 w-3" />
      </button>

      {open && (
        <div className="absolute right-0 top-full z-50 mt-1 w-48 rounded-xl border border-ink-200 bg-white py-1 shadow-base">
          {models.map(model => (
            <button
              key={model.modelName}
              onClick={() => { onChange(model.modelName); setOpen(false); }}
              className={`flex w-full items-center justify-between px-3 py-2 text-sm hover:bg-ink-50 ${
                model.modelName === value ? "bg-brand-50 text-brand-700" : "text-ink-700"
              }`}
            >
              <span>{model.displayName}</span>
              {model.modelName === value && <IconCheck className="h-4 w-4" />}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

#### 9. 改动 `features/ai/ui/AiChatPanel.tsx`

在 Header 区域（line 972-998 附近）现有模式切换旁边加 ModelSelector：

```tsx
const [selectedModel, setSelectedModel] = useState(() =>
  localStorage.getItem("preferredModel") ?? "agnes-2.5-flash"
);

// 在现有 AI_MODE_OPTIONS segmented control 后面加
<ModelSelector
  value={selectedModel}
  onChange={(model) => {
    setSelectedModel(model);
    localStorage.setItem("preferredModel", model);
  }}
/>
```

模型偏好用 `localStorage` 存储，不走服务端（第一版够用）。后续接中转平台 Stage 2 `AiAccessKey` 表。

---

### Stage 4：LangGraph model-select 节点

#### 10. `features/ai/llm/model-routing.ts`

```typescript
import type { TaskType } from "./types";

/**
 * 根据任务类型 + 用户配置，决定使用哪个模型。
 * 优先级：manualOverride > 用户路由配置 > 系统默认值
 */
export function selectModel(
  taskType: TaskType,
  userConfig?: { manualOverride?: string; defaults?: Record<TaskType, string> }
): { providerId: string; modelName: string } {
  // 1. 手动覆盖优先（用户在前端手动选择）
  if (userConfig?.manualOverride) {
    return parseModelRef(userConfig.manualOverride);
  }

  // 2. 用户路由配置
  if (userConfig?.defaults?.[taskType]) {
    return parseModelRef(userConfig.defaults[taskType]);
  }

  // 3. 系统默认值（按任务复杂度）
  const defaults: Record<TaskType, string> = {
    quick: "agnes:agnes-2.5-flash",
    chat: "agnes:agnes-2.5-flash",
    search: "agnes:agnes-2.5-flash",
    rag: "agnes:agnes-2.5-flash",
    complex: "deepseek:deepseek-chat",
  };

  return parseModelRef(defaults[taskType] ?? defaults.chat);
}

function parseModelRef(ref: string): { providerId: string; modelName: string } {
  const [providerId, modelName] = ref.split(":");
  return { providerId, modelName };
}
```

#### 11. `features/ai/graph/nodes/model-select.ts`（新增）

```typescript
import type { AgentState } from "../agent";
import { selectModel } from "@/features/ai/llm/model-routing";
import type { TaskType } from "@/features/ai/llm/providers/types";

export async function modelSelectNode(
  state: AgentState
): Promise<Partial<AgentState>> {
  // 从 detectIntent 已经解析出 taskType
  const taskType = (state as any).queryType as TaskType ?? "chat";

  // 读取用户配置（从请求 context 传入，或从 DB/localStorage 读取）
  const userConfig = (state as any).modelContext?.userConfig;

  const { providerId, modelName } = selectModel(taskType, userConfig);

  return {
    modelContext: {
      taskType,
      providerId,
      modelName,
    },
  };
}
```

#### 12. 改动 `features/ai/graph/state.ts`

在 `AgentStateAnnotation` 加：

```typescript
modelContext: Annotation<{
  taskType: TaskType;
  providerId: string;
  modelName: string;
  userConfig?: UserRoutingConfig;
} | null>({
  value: (current, update) => update === undefined ? current : update,
  default: () => null,
}),
```

#### 13. 改动 `features/ai/graph/agent.ts`

在 `detectIntent` 后面加 `modelSelect` 节点：

```typescript
.addNode("modelSelect", modelSelectNode)
.addEdge("detectIntent", "modelSelect")
// modelSelect 后面的边不变，继续走各工具节点
```

---

### Stage 5：API 支持 modelName 参数 + generate-response 动态模型

#### 14. 改动 `features/ai/graph/nodes/generate-response.ts`

将硬编码的 `withAgnetModelFallback` 改为动态模型：

```typescript
// 不再 import { withAgnetModelFallback } from "@/features/ai/llm/agnes-provider"
// 改为从 registry 创建
import { createModel } from "@/features/ai/llm/providers/registry";

export async function generateResponseNode(state: AgentState) {
  const taskType = state.modelContext?.taskType ?? "chat";
  const manualOverride = (state as any).configurable?.manualOverride;
  const userConfig = { manualOverride };

  const { providerId, modelName } = selectModel(taskType, userConfig);
  const model = createModel(providerId, modelName);

  const result = await generateText({
    model,
    system: systemPrompt,
    messages,
  });

  return { response: result.text };
}
```

#### 15. 改动 `app/api/ai/conversations/[id]/messages/route.ts`

```typescript
// 从请求体读 modelName（前端手动切换时传入）
const modelName = body.modelName;
// 从请求体读 manualOverride（覆盖路由）
const manualOverride = body.manualOverride;

// 传给 graph
const result = await agentGraph.invoke(
  { messages, mode, ... },
  {
    configurable: {
      modelName,
      manualOverride,
    },
  }
);
```

---

## 与中转平台各 Stage 的对应关系

| 本需求 Stage | 对应中转平台 Stage | 说明 |
|------------|-----------------|------|
| Stage 1 | Stage 6 | `model-provider.ts` / `model-catalog.ts` / `model-runtime-config.ts` 完全对齐 |
| Stage 2 | Stage 4 | Settings UI 参考 settings-api-keys block |
| Stage 3 | Stage 4 | 前端 ModelSelector 参考 shadcn-aisdk-model-select |
| Stage 4 | Stage 6 | model-select 节点 + model-routing.ts |
| Stage 5 | Stage 6 | 灰度开关 + 动态 providerId |

**中转平台 Stage 6 实施时，本需求的代码 100% 可复用**，无需重构。

---

## 与现有 Workflow Agent 方案的关系

**两个需求独立，可并行实施**：

| 需求 | 改动范围 | 优先级 |
|------|---------|--------|
| AI 模型配置层 | `features/ai/llm/` + LangGraph graph | ⭐⭐⭐⭐ |
| Workflow Agent（工作模式） | `features/ai/workflow/` 新目录 | ⭐⭐⭐⭐（长期） |

---

## 暂未决定（待你确认）

1. **DeepSeek API Key 存哪？**
   - 选项 A：`.env.local`（用户手动配，快速上线）→ **推荐第一版**
   - 选项 B：DB `user_ai_configs` 表（用户界面配置，复杂）
   - 选项 C：接中转平台 Gateway（Stage 6+，长期）

2. **用户路由配置存哪？**
   - 选项 A：`localStorage`（简单够用）→ **推荐第一版**
   - 选项 B：中转平台 Stage 2 DB 表

3. **TaskType 怎么从 detectIntent 解析？**
   - `chat`/`quick`：普通对话，无关键词
   - `search`/`rag`：含搜索关键词（"查一下"、"搜索"）
   - `complex`：含"分析/为什么/结合"等复杂推理词

---

## 改动摘要

| 文件 | 操作 | 对齐中转平台 |
|------|------|------------|
| `features/ai/llm/providers/types.ts` | 新增 | Stage 6 |
| `features/ai/llm/providers/system-providers.ts` | 新增（拆分） | 现有 agnes-provider.ts 迁移 |
| `features/ai/llm/providers/user-providers.ts` | 新增 | — |
| `features/ai/llm/providers/registry.ts` | 新增 | Stage 6 `model-provider.ts` |
| `features/ai/llm/model-catalog.ts` | 新增 | Stage 6 |
| `features/ai/llm/model-runtime-config.ts` | 新增 | Stage 6 |
| `features/ai/llm/model-routing.ts` | 新增 | — |
| `features/ai/llm/model-selector.tsx` | 新增 | Stage 4 |
| `features/ai/llm/agnes-provider.ts` | 删除 | 合并到 system-providers.ts |
| `features/ai/graph/state.ts` | 修改 | 加 modelContext |
| `features/ai/graph/nodes/model-select.ts` | 新增 | Stage 6 |
| `features/ai/graph/nodes/generate-response.ts` | 修改 | 动态模型 |
| `features/ai/graph/agent.ts` | 修改 | 加 modelSelect 节点 |
| `app/api/ai/models/route.ts` | 新增 | Stage 3 API |
| `features/settings/SettingsCenter.tsx` | 修改 | Stage 4 UI |
| `features/ai/ui/AiChatPanel.tsx` | 修改 | 加 ModelSelector |