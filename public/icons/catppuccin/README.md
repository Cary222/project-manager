# catppuccin file icons

File-explorer icons served from `/icons/catppuccin/{latte,mocha}/`.

Source: [catppuccin/vscode-icons](https://github.com/catppuccin/vscode-icons) (MIT), `main` branch `icons/` directory. Only the 32 names referenced by `FileIcons.tsx` are vendored here; re-fetch from the repo if a new icon name is added.
